<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body { background-color: #f8f9fa; }
        .bike-img { width: 80px; height: 60px; object-fit: cover; border-radius: 6px; }
        .navbar { background: linear-gradient(135deg, #1a1a2e, #16213e) !important; }
        .toast-container { position: fixed; top: 80px; right: 20px; z-index: 9999; }
        .preview-img { max-height: 200px; object-fit: contain; border-radius: 8px; border: 2px dashed #dee2e6; }
    </style>
</head>
<body>
